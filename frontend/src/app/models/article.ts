export interface Article {
  country      : string;   // “china”, “japan”, etc.
  title        : string;
  url          : string;
  publishedAt  : string;   // o Date si en el back la devuelves como ISO
}
